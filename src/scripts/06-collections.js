let myBtn = document.querySelector('#testButton');
let list = document.querySelector('ul');
const myDivElem = document.querySelector('.js-block');
/* 
Властивість innerHTML
 - Читання
 - Зміна
 - Додавання
 - insertAdjacentHTML
*/

/* console.log(list.innerHTML);
console.log(myDivElem.innerHTML);

let markup = [];
for (let i = 0; i < 10; i++) {
  const elem = `<li>${i}</li>`;
  markup.push(elem);
}

myDivElem.innerHTML = markup.join('\n');
list.innerHTML = markup.join('\n');
 */

// function onBtnClick() {
//   list.innerHTML = '';
// }
// myBtn.addEventListener('click', onBtnClick);

// ==========================
